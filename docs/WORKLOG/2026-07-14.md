# Worklog — 2026-07-14

Chronological record of the day's engineering sessions on this repo. See linked docs for full detail on each item; this file is a timeline, not a duplicate of the canonical status docs.

6. **P0 production correction: public [PARENT_COUPON_CODE_SUPERSEDED] advertising removed; Audience Coupon Policy frozen.** The Complete Collection page publicly advertised an [PARENT_COUPON_CODE_SUPERSEDED] coupon code — a real inconsistency with the Frozen Funnel Constitution's principle that audience coupons are conversion tools, never public marketing offers. **Inventory**: repo-wide search for [PARENT_COUPON_CODE_SUPERSEDED]/[EDUCATOR_COUPON_CODE_SUPERSEDED]/[GIFT_BUYER_COUPON_CODE_SUPERSEDED] found exactly one public-facing occurrence — `plugins/brave-hearts-bundle-pricing/includes/bundle-landing-page.php` — plus unrelated backend coupon-validation logic in `bundle-cart.php`, which correctly stays. Confirmed via direct database query that the Complete Collection page itself has zero literal coupon text in its stored content — the page is fully PHP-rendered, so the inventory is complete. **Fix**: removed the flagged line and its now-unused CSS rule, no replacement discount messaging added. Confirmed via WP-CLI that the underlying WooCommerce [PARENT_COUPON_CODE_SUPERSEDED] coupon is completely unchanged. **Deployment**: plugin version bumped 1.8.2 → 1.8.3 for cache-busting, deployed to staging then production, zero PHP fatals, zero remaining coupon-code text. **QA**: live-verified on both environments (desktop + mobile) — Complete Collection page, homepage, and a sitewide search for "[PARENT_COUPON_CODE_SUPERSEDED]" (zero results) all confirmed clean. **Documentation**: added a permanent, Frozen **Audience Coupon Policy** to `ENGINEERING/FUNNEL_CONSTITUTION.md` and `DECISIONS.md`. Updated pointer docs. The Parent Funnel Mailchimp build (item 5, below) was not touched this item — it resumes from its existing checkpoint next.

5. **Audience Routing Constitution recorded; Parent - Acquisition Funnel trigger bug fixed and Email 1 fully built.** Continuing from item 4's checkpoint. Wrote the permanent Audience Routing Constitution into `ENGINEERING/FUNNEL_CONSTITUTION.md` and `DECISIONS.md`. **Live-verified `Parent - Acquisition Funnel` (id 89) and found a real bug**: the trigger step's Tag field was genuinely empty (disabled Save button). Fixed by selecting the existing tag and saving — confirmed via the flow map. Built Email 1 (PDF delivery) in full: subject, preview text, body, and a working download button linked to the live PDF URL — fixed two authoring bugs found via the live preview panel (a stray merge-tag artifact, an unlinked button). **Tooling finding, distinct from the already-documented click-vs-drag issue**: dragging a step onto the "+" node between an *already-populated* step and "Contact exits" did not register, across multiple attempts including a controlled test with a proven-working draggable item, which succeeded on the trigger's initial empty placeholder but failed on this second target. Flagging for a fresh session to retry with a different technique. **Stopped here, not completed this session**: delay, Email 2, purchase-sync buffer, Conditional Split, Email 3, testing, contact migration, old-flow retirement, and the two post-purchase automations all remain outstanding. `Parent - Acquisition Funnel` is still safely a Draft with only trigger + Email 1 populated.

4. **Global purchaser tagger built and activated; Standard plan verified; consolidated Parent journey started.** Continuing from item 3's pause: built `Global - Tag Purchasers` (trigger: contact buys any product from the store; action: add tag `Customer - Purchased`; re-entry disabled; no historical backfill), activated it, confirmed 0/0/0 immediately after activation and unchanged on reload. Root-caused the earlier stuck UI: Mailchimp's automation-builder Actions palette is drag-and-drop, not click-to-add — this explains every earlier failed click in item 3. Checked WooCommerce order sync via the plugin's debug logs directly: confirmed orders #374 and #336 (both still "Processing," never "Completed") synced successfully to Mailchimp via `addStoreOrder` within seconds of entering Processing — paid Processing orders do reach Mailchimp promptly, fulfillment completion is not the gate. Also found the "2 Orders" dashboard figure is unrelated to order status: orders placed while logged in as WordPress user #1 (Administrator) are skipped from sync entirely (`order_process :: Order #XXX skipped, user #1 user role is not in the list`), including a Completed order (#322) — administrator test orders must not be used as purchase-sync proof. Attempted to add a native If/Else purchase check to the live `Coupon Flow`: paused it first (Mailchimp's own dialog confirms delay timers keep counting while paused, contacts aren't disrupted), dragged the If/Else into the map, and hit a real plan-tier wall — the account's Essentials plan caps Customer Journeys at 4 total steps, and the flow had already used all 4 (trigger+delay+email+the new If/Else) with zero left for real branch actions ("Upgrade for Branching"). Removed the uncommitted If/Else cleanly (confirmed via Mailchimp's own delete dialog and a reload) and left Coupon Flow deliberately Paused rather than resuming it unprotected, since a live segment check found 4 of the 9 Parent-tagged contacts already have purchase activity and none had `Customer - Purchased` yet (no backfill). Verified the Essentials-vs-Standard question directly (not guessed): Billing Plans page confirmed the account was genuinely on Essentials Annual ($120/yr), and the plan-comparison table named the exact mechanism — Essentials allows up to 4 Customer Journey points, Standard up to 200. **Andrew manually upgraded the account to Standard Annual** ($192/yr) outside this session. Re-verified live: Billing Plans now shows STANDARD ANNUAL, 6,000 email sends (up from 5,000); all 6 existing automations unchanged (Global - Tag Purchasers Active 0/0/0, Coupon Flow still Paused 3/3/0, Reluctant Reader Adventure Kit and MT Lead Magnet unaffected); the Coupon Flow builder's step-cap banner is now gone entirely. Began Phase 3 of the consolidation directive: created new Draft automation `Parent - Acquisition Funnel` (id 89) with its trigger configured (tag added: Reluctant Reader Adventure Kit, matching the existing Parent entry tag) — confirmed persisted after reload. **Stopped here, not completed this session:** Email 1/2/3 content, the delay/purchase-sync buffer, the Conditional Split itself, testing, contact migration, retiring the old split flows, the two 45-day post-purchase automations, and full documentation closeout all remain outstanding — this is genuinely multiple more sessions of careful, verified work, not something to compress into one pass given this session's demonstrated Mailchimp-UI instability (repeated screenshot timeouts).

2. **Frozen Funnel Architecture established — permanent company policy.** Andrew, acting as CSO, issued the complete Brave Hearts Funnel Constitution as a single consolidated production prompt: every current and future audience funnel must follow one canonical sequence (Traffic Source → Landing Page → Popup → Lead Magnet → Email 1 delivery → Email 2 result-sell with no coupon → Purchase Decision → [purchased: post-purchase nurture, tracked as full-margin conversion] / [not purchased: Email 3 with audience coupon] → 30-60 day follow-up), must suppress the coupon for anyone who purchases before Email 3, and must be built as reusable automation modules (Email 1 / Transformation Email / Purchase Check / Coupon / Post-Purchase / Long-Term Nurture) rather than one-off audience-specific workflows. Recorded verbatim in new file `docs/ENGINEERING/FUNNEL_CONSTITUTION.md`; pointers added to `DECISIONS.md`, `AUDIENCE_FUNNEL_ARCHITECTURE.md`, `START_HERE.md`, `PROJECT_STATE.md`, `AI_CONTEXT_INDEX.md`, `.claude/rules/funnels.md`. This architecture is permanent — future funnels extend it, never replace it with a parallel system.

3. **Mailchimp capability audit (Chrome, Andrew's authenticated session).** Confirmed Andrew's real Chrome session is logged into Mailchimp (Essentials Annual plan, 500 contacts, active since 2026-05-15). Live-audited the actual account state via the Claude-in-Chrome extension: two automations already exist and are active — "Reluctant Reader Adventure Kit" (Email 1 + Email 2, tag-triggered, 5 started/4 completed/1 in progress) and "Coupon Flow" (same tag trigger, 10-day delay, then "Coupon Email - [PARENT_COUPON_CODE_SUPERSEDED]", 3 started/3 in progress/0 sent — expected, since the flow only relaunched 2026-07-11 and the delay hasn't elapsed). **Two real gaps found:** (1) live "Email 2" copy ("Maybe your child does not hate reading...") does not match Andrew's approved "Chapter 7" result-focused copy from the prior sprint; (2) Coupon Flow is a linear 3-step automation with no purchase-check branch — it will send [PARENT_COUPON_CODE_SUPERSEDED] to any tagged contact after 10 days regardless of purchase status, violating the new Purchase Suppression Rule. **Capability resolved:** built and then deleted a temporary draft automation (never activated, zero contacts, safe) specifically to inspect the trigger catalog — confirmed genuine, live WooCommerce store data is connected to Mailchimp (purchase-based triggers "Buys any product," "Buys a specific product," and "Order confirmation" are real, not "connect to use" placeholders), and the platform's own contextual help references "conditional split rules" as an available feature on this account. This resolves the sprint's explicit STOP CONDITION check — purchase suppression is buildable here, not a plan-tier blocker. **Paused before further live changes:** this exact browser-automation UI proved unreliable during the session (repeated failed clicks, 30-second render timeouts on the canvas-based flow builder) — flagged to Andrew before attempting live edits to the two active automations (one currently has 3 real contacts mid-flow). Awaiting the next continuation to build the purchase-tagging automation and wire suppression into Coupon Flow, per Andrew's "one verified step at a time" instruction.

1. **Audience Funnel System Sprint 1B — Parent Funnel production deployment + Mailchimp gate.** Andrew directed completing, deploying, and validating the full Parent Funnel end-to-end, introducing an "Interactive Authentication Rule": stop at the exact point a login is needed and ask Andrew directly rather than treating it as a deferred blocker. **Phase 1 preflight:** confirmed clean working tree, local HEAD matched origin exactly, both prior commits (`6e8a0ad`, `3607bcc`) present, staging v1.19.14 vs production v1.19.13 confirmed as expected. Diffed production's live files against the pre-sprint git baseline — confirmed the only drift was the already-known, pre-existing, unrelated WooCommerce coupon-CSS gap (documented in `KNOWN_ISSUES.md` since 2026-07-13), no other unexpected changes. **Phase 2:** deployed `page-reluctant-reader-adventure-kit.php` and `style.css` to production after capturing a private backup outside the repo; verified zero PHP fatals, active theme version 1.19.14, cache purged; live QA confirmed the Collection/Trust/FAQ sections and hero secondary CTA all render correctly on production desktop and mobile, zero console errors, zero horizontal overflow. **Phase 3:** confirmed the existing Parent popup already satisfies the sprint's functional requirements (correctly excluded from the landing page itself, since that page has its own inline form — a deliberate, sensible pre-existing design, not a gap) and still renders correctly sitewide (verified on the homepage: exactly 1 popup element, correct config). Preserved existing approved popup copy per instruction rather than rewriting it to match the directive's "recommended" headline; flagged two minor copy gaps (doesn't name "The Mariana Trench" specifically, doesn't explicitly state the signup begins an email journey) as findings, not fixed without explicit authorization. **Phase 4:** re-confirmed the lead-magnet PDF URL is still correctly configured and unchanged. **Phase 12:** re-verified the Complete Collection destination live on production — all 6 core products confirmed in stock, exactly 6 published products (no legacy/duplicate), correct pricing ($31.99/$48.99), correct [PARENT_COUPON_CODE_SUPERSEDED] Collection-only messaging, zero console errors. **Phase 13 (partial):** deliberately reviewed the `parent_landing_view`/`bundle_page_view` consent-gating discrepancy this sprint's own brief called out — concluded the correct fix (gating `bundle-landing.js` the same way `should_render_analytics()` gates everything else) belongs in an isolated future pass on the separate `brave-hearts-bundle-pricing` plugin, since this sprint's own scope rules forbid touching unrelated plugin systems; did not weaken any consent/privacy rule to reach this conclusion. Documented in `KNOWN_ISSUES.md`. **Stopped at Phase 5 (Mailchimp login and current-state audit)** per the sprint's explicit Interactive Authentication Rule — everything from Phase 5 through Phase 11 (tagging, Email 1/2/3, purchase-suppression logic, post-purchase nurture) depends on real Mailchimp account access, which no tool in this environment can reach and which this directive explicitly says must not be treated as a deferred blocker. Asked Andrew to log into Mailchimp and confirm before resuming. See the in-session report for the exact ask.
