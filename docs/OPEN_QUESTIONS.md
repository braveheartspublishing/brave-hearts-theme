# Open Questions

Questions requiring Andrew's or ChatGPT/CSO's judgment call — not decided unilaterally by engineering. Remove an entry once answered and the decision is recorded in `DECISIONS.md` (if durable) or the relevant status doc.

## Legacy Blog Conversion Sprint 1 (posts 76 & 68) — 2026-07-13

**Resolved (2026-07-13):** post 68's book framing — Andrew's approved copy keeps the Mariana-Trench-specific framing (does not broaden to series/Collection language, per his explicit instruction "Do not broaden the article toward the Complete Collection"). No longer open.

1. **Amazon affiliate attribution verification is pending on multiple posts' Amazon links** (`dib_tag=se` present, but no literal `tag=` parameter) — confirmed on posts 68, 26, 66, and 30's Amazon-affiliate URLs (all share the same `dib_tag=se` pattern with no real `tag=`). Affects multiple posts, not just one — independent of the link-remediation work in these sprints. Flagging for Andrew to verify — not something engineering should silently "fix" by fabricating a tag value. (Note: posts 76, 26, and 30's book links no longer point to Amazon at all — they were swapped to the direct on-site product page, which sidesteps this issue for those three going forward; posts 66 and 68's remaining Amazon links still carry it.)
2. **`BHP_CTA_Collision_Detector` code-vs-policy gap:** `detect_contextual_links()` only scans `<p>` tags (misses links inside `<h4>` or other non-`<p>` elements) and its Amazon-affiliate pattern requires a literal `tag=` query parameter (misses real Amazon links that use other tracking params like `dib_tag=`). Found while evaluating post 68's real, working links against the actual enforcement code, not just the plain-language policy. Not fixed this sprint — worth a small, separate engineering task if Andrew wants the automated detector to actually recognize these link types.
   - **Extended finding, 2026-07-13 (Batch 2 copy implementation):** `TOPIC_HUB_URL_PATTERNS` in the same class only matches `/blog/category/...`, `/blog/tag/...`, and literal `/mariana-trench/` or `/bridge-books/` path segments — it has never recognized the `/teachers/#reading-growing` / `/teachers/#science-geography` anchor-hash URLs that are the *actual* hub destination used by every topic-hub link added across this project (Sprint 1's posts 76/68 and Batch 2's posts 26/66/30 alike). Confirmed by running `BHP_CTA_Collision_Detector::check()` directly against all three Batch 2 posts: it reported `topic_hub_link_present => false` on all three despite genuine, policy-compliant in-body contextual sentences linking to those exact hub pages (manually verified live in a real browser — correct anchor text, real surrounding prose, HTTP 200 destinations). Also found: the anchor-matching regex `/<a\s+href="([^"]+)"[^>]*>/` requires `href` to be the *first* attribute on the `<a>` tag — it silently fails to match links where `target="_blank"` appears before `href` (post 30's pre-existing book-discovery link uses that attribute order and is invisible to the detector as a result). Net effect: the automated `required_links` gate has likely reported false "fail" states on every article this project has ever produced that used a `/teachers/#...` hub link or a `target`-before-`href` anchor — a real, systemic detector gap, not a content-compliance problem. Not fixed this task (out of the approved copy-only scope); worth folding into the same future engineering task as item 2 above.

## Legacy Blog Conversion Batch 2 (posts 26, 66 & 30) — 2026-07-13 overnight

3. **Posts 66/74/94 cover overlapping "Mariana Trench facts" territory** (a real, pre-existing cannibalization pattern, not introduced by this batch). Needs an editorial decision (consolidate, differentiate more sharply, or accept the overlap) before further link/copy work on any of the three — not resolved here.
4. **Post 30 sits inside a much larger "bridge books" content cluster** — at least 9 other published posts (`50`, `88`, `54`, `90`, `48`, `60`, `52`, `96`, `64`) cover closely related territory. Potential content consolidation is tracked in the private CSO knowledge base. Recommend Andrew/CSO revisit that before approving new topic-hub copy for post 30 specifically, since a consolidation decision could change what "the right link" even means for this post.

**Resolved (2026-07-13):** both open items from the Hardcover Fulfillment Verification sprint are closed. Andrew established the print-on-demand stock policy (see `DECISIONS.md`'s "Print-on-demand stock policy" entry) — all 3 hardcover products restored to `instock`, confirmed live. Andrew also confirmed deletion of draft product 338 (empty, broken shell, zero sales) — permanently deleted, backed up first. No longer open.
