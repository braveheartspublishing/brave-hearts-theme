# Blog Status

**36 published posts** on production (verified via WP-CLI, 2026-07-12).

- 35 registry-curated posts (in `bhp_get_guide_registry()`, get the full guide-continuation experience with related-articles grid and hub links).
- 1 non-registry post (`amazon-rainforest-facts-for-kids`) — falls back to the CTA Engine's automatic end-of-article CTA instead of the curated guide-continuation block.
- Every article requires a contextual topic-hub link and a contextual book-discovery link in-body (enforced by `BHP_Required_Links_Gate` in the content-QA pipeline, staging-only currently — see `docs/PROJECT_STATE.md`).

## Weekly content cycle
Governed by the canonical `docs/content-operations/` docs — weekly opportunity scoring, slate persistence, article-assignment packets, and the full editorial pipeline are built in the separate `brave-hearts-seo-engine` repo, not tracked in per-post detail here. See that repo's own documentation for exact cadence and current slate status.

## Legacy content audit (2026-07-13)
The 35 pre-CTA-Engine posts were audited for contextual-link coverage and book-relevance — see `CONTENT/LEGACY_BLOG_CONVERSION_AUDIT.md` for the full matrix. Headline findings: the automatic `bhp_get_guide_registry()` "Related Field Notes" grid + hub link renders on every registry post regardless of content, so topic-hub-link coverage is likely near-universal already; book-discovery links vary and mostly default to the Mariana Trench Amazon listing; **only 1 of 35 posts mentions "The Amazon"** (the third book, published 2026-06-26, after this content was written) — the biggest real, actionable gap. 5 remediation packets prepared, not implemented — staging-first per standing workflow.

## Legacy Blog Conversion Sprint 1 — posts 76 & 68 (2026-07-13)
Status: **production complete.** Approved copy implemented and QA-passed on staging, then deployed to production the same day (overnight build), live-verified in a real browser with zero regressions, SiteGround cache purged. Full detail in `CONTENT/LEGACY_BLOG_CONVERSION_AUDIT.md`'s "Sprint 1" section. Post 76 gained a real in-body topic-hub link and its book-discovery link now points directly to the on-site product page instead of Linktree. Post 68 gained a real in-body topic-hub link and its Amazon book-discovery link was replaced with a direct on-site Mariana Trench product link and descriptive anchor text. Also surfaced a real gap between `BHP_CTA_Collision_Detector`'s regex-based enforcement and the plain-language required-links policy (only `<p>` tags scanned; Amazon-affiliate pattern requires a literal `tag=` parameter) — not fixed this sprint, flagged for engineering in `OPEN_QUESTIONS.md`.

## Legacy Blog Conversion Batch 2 — posts 26, 66 & 30 — FULLY PRODUCTION-COMPLETE (2026-07-13)
Status: **both the approved topic-hub copy and the earlier mechanical fixes are now live on production for all three posts.** Full 20-point packets in `CONTENT/LEGACY_BLOG_CONVERSION_AUDIT.md`'s "Batch 2" section; implementation detail in that doc's "Batch 2 copy implementation" and "Batch 2 mechanical production closeout" sections. Andrew approved final copy for the one remaining copy requirement on each post (a new topic-hub sentence linking "Explorer Expedition Guides" to the `reading-growing`/`science-geography` hub, plus a book-title link swap on post 66) — implemented on staging then production. Separately, the earlier staging-tested mechanical fixes (posts 26/30's Amazon→on-site book-link URL swap, post 66's malformed split-anchor repair in its "Related Reading" list) were then deployed to production too, confirmed byte-identical to the staging reference version and live-verified with zero regressions (correct links, repaired HTML, unchanged CTA/related-content blocks, zero console/PHP errors, no new CTA collision). A real, previously-undiscovered gap in `BHP_CTA_Collision_Detector`'s automated required-links check was found and confirmed during this work — its hub-URL pattern list has never recognized `/teachers/#...` anchor-hash URLs (the actual destination format used by every hub link this project has ever added), and its anchor-extraction regex is fragile to attribute ordering; compliance for this batch was confirmed by manual live-browser verification against the policy's own written definition instead. See `OPEN_QUESTIONS.md` and `KNOWN_ISSUES.md` — flagged as a future engineering task, not a publishing blocker. Two real, pre-existing content-cannibalization clusters remain unresolved (Mariana Trench facts: posts 66/74/94; bridge books: post 30 plus 9+ others) — flagged in `OPEN_QUESTIONS.md`, not resolved.

## Malformed Amazon link fix — posts 38, 64, 88, 90 (2026-07-13)
Status: **fixed and verified on production.** Discovered during Conversion QA Sprint 1: 4 posts had doubled-protocol `href="https:// https://amzn.to/..."` links (7 total occurrences) resolving to broken addresses. Fixed via deterministic replacement, byte-diff-verified, zero regressions, zero new CTA collisions. See `CONTENT/LEGACY_BLOG_CONVERSION_AUDIT.md`'s "Malformed doubled-protocol Amazon link fix" section.

## Sitewide mechanical-defect scan (2026-07-13 overnight, read-only)
A pattern-based scan of all 36 published posts' raw content found at least one flagged pattern (Linktree links, weak/generic anchor text, `<h4>`-isolated CTA links instead of in-body prose, Amazon links missing a real Associates `tag=` parameter, possible malformed split-anchors) in 35 of 36 posts. A prioritized P0–P3 fix queue was built — see `CONTENT/LEGACY_BLOG_CONVERSION_AUDIT.md`. No posts beyond 76/68/26/66/30 were edited; this is a planning artifact for future small sprints. Corpus-wide, essentially every sampled Amazon affiliate link shares the same pattern — Amazon affiliate attribution verification is pending sitewide, flagged for Andrew.

## Known content-accuracy flags (editorial, not technical)
- Individual product pages say "Printed and shipped by Lulu" (real partner is Bookvault) while the Collection page says "Printed and shipped in the USA" — inconsistent, not corrected pending an editorial decision.
- The Collection page's "Used in 40 classrooms" claim has no on-hand verification.
