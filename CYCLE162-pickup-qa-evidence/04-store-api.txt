STORE API EVIDENCE — the rate as the React checkout actually receives it
staging, theme 1.19.231 / bundle 1.8.49, 2026-08-17
Session flagged with ?bhp_visit=liberty-2026-09-04, cart = 1 Mariana paperback,
destination Dallas TX 75001. Requests dispatched internally via rest_do_request(),
which runs the identical route handlers a browser hits.

STORE API GET /wc/store/v1/cart (what the React checkout renders from)  HTTP 200
  rate_id=flat_rate:1        method_id=flat_rate          selected=true  price=199  name="Contiguous US Shipping"
        meta: Items = Adventures of Charlotte and Henry: The Mariana Trench (Paperback) - Perfect Bound &times; 1
  rate_id=bhp_school_pickup  method_id=bhp_school_pickup  selected=false price=0  name="Author hand-delivery at the Liberty visit (September 4)"
        meta: School = Liberty
        meta: Visit date = 2026-09-04
  totals: total_shipping=199 total_price=1398 (minor units, USD)
  errors in payload: none

--- select the pickup rate through the exact route the React checkout POSTs to ---
STORE API POST /wc/store/v1/cart/select-shipping-rate  HTTP 200
  rate_id=flat_rate:1        selected=false price=199  name="Contiguous US Shipping"
  rate_id=bhp_school_pickup  selected=true  price=0  name="Author hand-delivery at the Liberty visit (September 4)"
  totals: total_shipping=0 total_price=1199 (minor units)
  errors in payload: none
wc notices: 0

WHAT THIS PROVES: the custom rate is serialized correctly by the Store API
schema, carries its School/Visit-date metadata, is selectable through the
select-shipping-rate route, and drives total_shipping to 0 with no error in the
payload and no WooCommerce notice raised.

WHAT THIS DOES NOT PROVE: that the radio renders correctly in the React UI at
any viewport, or that the page throws no console error. No browser was used.

FOOTNOTE ON METHOD: the first attempt at the POST returned HTTP 401
"woocommerce_rest_missing_nonce". That is the Store API refusing an unsigned
write, correctly, and not a defect in the rate. Re-issued with a real
wp_create_nonce('wc_store_api') header, which is what the browser sends.
